# Signalint website

Static Astro website for [Signalint](https://github.com/TranQui004/signalint),
published independently from the MCP server repository.

## Development

```sh
npm install
npm run dev
npm run build
```

The production build is deployed to GitHub Pages from `main`.
