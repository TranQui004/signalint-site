---
import { fetchLiveNpmVersion } from '../data/liveData';

const base = import.meta.env.BASE_URL.replace(/\/?$/, '/');

interface Props {
	stickyReveal?: boolean;
	locale?: 'en' | 'vi';
}

const { stickyReveal = false, locale = 'en' } = Astro.props;
const prefix = locale === 'vi' ? 'vi/' : '';
const currentVersion = await fetchLiveNpmVersion();
---

<footer class:list={['site-footer', { 'site-footer--sticky': stickyReveal }]} data-footer-reveal={stickyReveal ? '' : undefined}>
	<div class="shell footer-grid">
		<div data-footer-item={stickyReveal ? '' : undefined}>
			<p class="footer-kicker">{locale === 'vi' ? 'MCP cá»¥c bá»™ / stdio' : 'Local MCP / stdio'}</p>
			<p class="footer-title">{locale === 'vi' ? 'Giá»¯ cháº©n Ä‘oÃ¡n tháº­t sá»± há»¯u Ã­ch.' : 'Keep diagnostics useful.'}</p>
			<p class="footer-summary-note">{locale === 'vi' ? 'Signalint miá»…n phÃ­, mÃ£ nguá»“n má»Ÿ theo MIT License.' : 'Signalint is free and open source under the MIT License.'}</p>
		</div>
		<div class="footer-col" aria-label={locale === 'vi' ? 'LiÃªn káº¿t tÃ i liá»‡u' : 'Documentation links'} data-footer-item={stickyReveal ? '' : undefined}>
			<strong>{locale === 'vi' ? 'Tham kháº£o' : 'Reference'}</strong>
			<a href={`${base}${prefix}docs/`}>{locale === 'vi' ? 'TÃ i liá»‡u' : 'Documentation'}</a>
			<a href={`${base}${prefix}docs/tools/`}>MCP tools</a>
			<a href={`${base}${prefix}docs/architecture/`}>{locale === 'vi' ? 'Kiáº¿n trÃºc' : 'Architecture'}</a>
			<a href={`${base}${prefix}blog/`}>Blog</a>
		</div>
		<div class="footer-col" aria-label={locale === 'vi' ? 'LiÃªn káº¿t dá»± Ã¡n' : 'Project links'} data-footer-item={stickyReveal ? '' : undefined}>
			<strong>{locale === 'vi' ? 'Dá»± Ã¡n' : 'Project'}</strong>
			<a href="https://www.npmjs.com/package/signalint-mcp">npm package</a>
			<a href="https://github.com/TranQui004/signalint">GitHub</a>
			<a href="https://github.com/TranQui004">TranQui004 profile</a>
			<a href="https://github.com/TranQui004/signalint/issues">Issues</a>
			<a href={`${base}${prefix}changelog/`}>Changelog</a>
		</div>
	</div>
	<div class="shell footer-bottom" data-footer-item={stickyReveal ? '' : undefined}>
		<span>signalint-mcp@{currentVersion} / schema 1.2</span>
		<span>{locale === 'vi' ? 'JavaScript + TypeScript / phÃ¡t triá»ƒn cÃ´ng khai' : 'JavaScript + TypeScript / built in public'}</span>
	</div>
</footer>
